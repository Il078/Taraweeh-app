import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.il078.taraweeh',
  appName: 'Taraweeh App',
  webDir: 'out',
  server: {
    androidScheme: 'https'
  }
};

export default config;
